// Better-Auth handles middleware automatically, but we can add custom session handling here if needed
// For Docusaurus integration, we'll primarily use the client-side authentication functions

import { auth } from './auth.config';

export { auth };